% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/database.R
\name{optimize_db}
\alias{optimize_db}
\alias{optimise_db}
\title{Re-order the data in a database for faster processing}
\usage{
optimize_db(db, sensors = NULL, .progress = TRUE)

optimise_db(db, sensors = NULL, .progress = TRUE)
}
\arguments{
\item{db}{A database connection, as created by \code{\link[=create_db]{create_db()}}.}

\item{sensors}{A character vector of one or multiple sensors. Use
\code{sensors = NULL} for all sensors. See \link[mpathsenser]{sensors} for a
list of all available sensors.}

\item{.progress}{Logical; whether to show a progress bar. Defaults to \code{TRUE}.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Rewrites the sensor tables ordered by \code{participant_id} and \code{time}. DuckDB
stores columnar data and maintains per-block zonemaps; ordering the data
this way improves zonemap pruning (e.g. for time range queries) and
compression. Run this after large imports for best query performance.
}
\examples{
\dontrun{
# Open a database connection
db <- open_db("path/to/db")

# Re-order all sensor tables
optimize_db(db)

# Or only the Accelerometer table
optimize_db(db, sensors = "Accelerometer")

# Remember to close the connection
close_db(db)
}
}
